{
  "name": "server-devtools",
  "version": "0.1.0",
  "type": "module",
  "bin": {
    "server-devtools": "dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup src/index.ts --format esm",
    "dev": "tsup src/index.ts --format esm --watch",
    "lint": "tsc --noEmit",
    "test": "node --test",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@server-devtools/instrumentation": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.13.4",
    "tsup": "^8.3.5",
    "typescript": "^5.7.3"
  }
}
