# basic-app

Minimal Next.js App Router app used to validate Server DevTools end-to-end.

## Run

```bash
pnpm install
pnpm dev:debug
```

- App: `http://127.0.0.1:3000`
- DevTools UI: `http://127.0.0.1:3002`

Click the button in the app to generate console output and server-side network requests.
